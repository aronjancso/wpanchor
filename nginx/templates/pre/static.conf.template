server {
    listen 80;
    server_name $STATIC_DOMAIN;

    root /var/www/static;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        try_files $uri =404;
    }
}
